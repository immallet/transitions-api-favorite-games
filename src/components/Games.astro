---
import GameItem from './GameItem.astro'
import { GAMES } from '../data/games'
---

<ul class='grid grid-cols-2 md:grid-cols-3 gap-3 px-4 py-2'>
  {
    GAMES.map((game) => (
      <li>
        <GameItem {...game} />
      </li>
    ))
  }
</ul>
